<% if flash[:notice] && flash[:notice] == 'Order was created' %>
    <div style="padding:0px 20px 40px 20px;text-align: center" id="modal-show" data-id="recall"><h2>Заявка принята</h2>Спасибо за обращение в Центр эпиляции и косметологии.<br>Наш менеджер скоро свяжется с вами для уточнения деталей.</div>
<% else %>
<%= form_for [Order.new] do |f| %>
  <div class="form-group">
    <label for="exampleInputPhone">Ваш телефон или email</label>
    <input type="text" class="form-control" id="exampleInputPhone" required="required" name="order[contacts]" style="width: 300px;">
  </div>
  <div class="form-group">
    <label for="exampleInputText"><%if defined?(text) %>Текст заявки<% else %>Комментарий (не обязательно)<% end %></label>
    <textarea style="height: 100px;" class="form-control" id="exampleInputText" name="order[content]" <%if not defined?(text) %>placeholder="Ваши пожелания по времени звонка и/или любые вопросы. Можете оставить незаполненым, если хотите просто записатсья на приём"<% end %>><%if defined?(text) %><%=text%><% end %></textarea>
  </div>
  <button type="submit" class="button">Отправить</button>
<% end %>
<% end %>